# Why do we need a virtual router in a Palo Alto firewall?

A virtual router maintains the routing table for Layer 3 interfaces. After traffic is permitted by the security policy, it determines the best path and next hop for forwarding the packet. Multiple virtual routers can be used to isolate routing domains on the same firewall.
